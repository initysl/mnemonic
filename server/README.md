# Mnemonic API

## Auth0 Configuration

Set the following environment variables for Auth0 JWT validation:

- `AUTH0_DOMAIN`
- `AUTH0_AUDIENCE`
- `AUTH0_ISSUER` (optional; defaults to `https://<AUTH0_DOMAIN>/`)
